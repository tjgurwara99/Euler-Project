name: C/C++ CI

on:
  push:
    branches: [ New ]
  pull_request:
    branches: [ New ]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - name: Doxygen Action
      uses: mattnotmitt/doxygen-action@v1.1.0
      with:
        # Path to Doxyfile
        doxyfile-path: ./Documentation # default is ./Doxyfile
        # Working directory
        working-directory: . # default is .
